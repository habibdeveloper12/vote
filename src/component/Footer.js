import React from "react";

const Footer = () => {
  return (
    <div
      className="bg-black "
      style={{
        padding: "15px",
        textAlign: "center",
        color: "white",
        marginTop: "20px",
      }}
    >
      Vote Site
    </div>
  );
};

export default Footer;
